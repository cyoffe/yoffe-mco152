package yoffe.physics;

public class ProjectileMain {

	public static void main(String[] args) {

		/*
		 * double angle = Math.toRadians(31); double velocity = 20; // per
		 * second double time = 2.7; // seconds
		 * 
		 * 
		 * Projectile aProjectile = new Projectile(angle, velocity, time);
		 * 
		 * for(int t=0; t < 10; t++){ aProjectile.setTime(t);
		 * System.out.println("X = " + aProjectile.getX() + "  Y = " +
		 * aProjectile.getY()); }
		 */

		final ProjectileGUI gui = new ProjectileGUI();
		gui.setVisible(true);

	}
}
